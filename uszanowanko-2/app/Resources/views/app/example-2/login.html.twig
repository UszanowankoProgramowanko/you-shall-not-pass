{% extends ':app:base.html.twig' %}

{% block header %}
    <a href="{{ path('example_2_main_page') }}">Main page</a>
{% endblock %}

{% block body %}
    {% if error %}
        <div>{{  error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    {{ form(loginType, {'action': url('example_2_login_check')} ) }}
{% endblock %}